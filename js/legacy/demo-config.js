/**
 * Demo Configuration
 * Optional configuration for demo/development purposes
 */

// Demo configuration is not needed for production
// This file exists to prevent 404 errors during initialization
console.log('📋 Demo config loaded (empty - production mode)');

// Export empty demo config
window.DEMO_CONFIG = {
    enabled: false,
    message: 'Demo mode disabled - running in production'
};
